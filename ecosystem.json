{
  "apps": [
    {
      "name": "nuxt-web",
      "autorestart": true,
      "script": "server/index.js",
      "env": {
        "COMMON_ENV_VAR": "true"
      },
      "env_production": {
        "NODE_ENV": "production"
      }
    }
  ],
  "deploy": {
    "production": {
      "user": "root",
      "host": ["49.233.165.116"],
      "port": "22",
      "repo": "git@github.com:SuperficialL/nuxt-web.git",
      "ref": "origin/master",
      "path": "/srv/nuxt-web",
      "post-deploy": "rm -rf node_modules && npm install && npm run build && pm2 startOrReload ecosystem.json --env production",
      "pre-deploy-local": "echo 'Deploy Done!'",  
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
